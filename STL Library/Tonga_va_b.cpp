#include<bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    unsigned long long a, b;
    cin >> a >> b;
    long sum = a + b;
    cout << sum << endl;
    return 0;
}